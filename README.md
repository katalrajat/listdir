# listdir
to check the content of any module
Steps to use this script
After downloading this file
Give permison 777 so everyone can use it
sudo chmod 777 filename
make a seprate directory do not use /usr/bin to store the command
sudo mkdir ~/bin
every one can access this  run
chmod 777 ~/bin of your choice"
move this file to ~/bin
and last run
export PATH=$PATH":$HOME/bin"
